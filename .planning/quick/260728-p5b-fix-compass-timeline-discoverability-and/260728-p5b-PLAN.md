---
quick_id: 260728-p5b
description: Fix Compass timeline discoverability and isolate per-note playback; audit adjacent playback/visibility defects
created: 2026-07-28
mode: quick-full-inline
status: complete
must_haves:
  truths:
    - Finishing or failing one voice note never starts another note.
    - A persisted older episode remains selected but visibly advertises the newer populated episode.
    - A failed saved-episode relay lookup falls back to the preferred latest episode instead of falling directly to cache/error UI.
    - The episode note index includes all notes regardless of the signed-in identity and labels each note distinctly.
  artifacts:
    - logbook-pwa/src/lib/playback.tsx
    - logbook-pwa/src/lib/initial-issue.ts
    - logbook-pwa/src/App.tsx
    - logbook-pwa/src/components/IssueTimeline.tsx
    - logbook-pwa/scripts/playback-qa.mjs
  key_links:
    - App startup uses the tested initial-issue resolver and exposes its newer-episode result.
    - Explicit track changes rotate the active audio element; terminal events clear only that generation's active-note state.
    - Browser QA drives the real React provider with deterministic issue and segment fixtures.
---

# Quick Task 260728-p5b Plan

## Context

Use the completed forensic report at `.planning/forensics/2026-07-28-compass-timeline-playback.md`. GSD specialist agents are not installed, so planning, execution, and validation run inline on the requested Sol model while retaining GSD quick-task artifacts and atomic commits.

## Task 1 — Lock playback isolation with a red browser regression

**Files:** `logbook-pwa/scripts/playback-qa.mjs`, `logbook-pwa/package.json`, `logbook-pwa/src/lib/playback.tsx`, `logbook-pwa/src/components/NoteRow.tsx`

**Action:** Add a deterministic Vite/Puppeteer harness with three fixture notes. Assert that clicking one note calls `play()` exactly once and that synthetic `ended` and `error` events do not start another source. Run it red against the current auto-advance behavior, then replace terminal auto-advance with a complete stop/reset and update stale queue comments.

**Verify:** `npm run test:playback` passes and reports zero new play invocations after terminal events.

**Done:** No path from `ended` or `error` calls `playSegment` for another note; toggling a different note still switches explicitly.

## Task 2 — Make persisted episode state explicit and resilient

**Files:** `logbook-pwa/src/lib/initial-issue.ts`, `logbook-pwa/src/lib/initial-issue.test.ts`, `logbook-pwa/src/App.tsx`, `logbook-pwa/src/App.css`

**Action:** Write red unit tests for saved-episode success, newer preferred-latest detection, saved lookup rejection fallback, and latest lookup degradation. Implement an injected initial loader, use it in App startup, and show a compact action banner when a newer populated episode exists. Selecting it must use the existing persistence and capability-reset path.

**Verify:** Focused Vitest tests pass; App compiles; no automatic episode switch occurs.

**Done:** Older persisted selections are understandable and recoverable, and one relay failure no longer suppresses the latest-episode fallback.

## Task 3 — Repair the signer-relative note index and audit adjacent behavior

**Files:** `logbook-pwa/src/lib/community-notes.ts`, `logbook-pwa/src/lib/community-notes.test.ts`, `logbook-pwa/src/components/IssueTimeline.tsx`, `logbook-pwa/src/App.public.test.tsx`, `logbook-pwa/src/App.css`

**Action:** Change the consolidated index from signer-relative “Other contributors” to a de-duplicated all-notes index. Give every anchor a distinct author-and-duration label, cover signed-in-own-note visibility and duplicate suppression, then audit adjacent playback, issue selection, loading, and empty/error states. Fix only defects demonstrated by focused tests or live harness evidence.

**Verify:** `npm run test:unit`, `npm run lint`, `npm run build`, and `npm run test:browser` all pass; independent diff review has no blocking findings.

**Done:** Signing in cannot remove notes from the index, identical author names no longer create indistinguishable links, and all project quality gates are green.
