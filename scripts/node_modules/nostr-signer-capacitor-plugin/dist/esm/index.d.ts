import type { Permission, SignerAppInfo as AppInfo } from './definitions';
export declare const buildPermissionsJson: (perms: Permission[]) => string;
export declare const NostrSignerPlugin: {
    setPackageName(packageName: string): Promise<void>;
    isExternalSignerInstalled(packageName?: string): Promise<{
        installed: boolean;
    }>;
    getInstalledSignerApps(): Promise<{
        apps: AppInfo[];
    }>;
    getPublicKey(packageName?: string, permissions?: Permission[] | string): Promise<{
        npub: string;
        package: string;
    }>;
    signEvent(packageName: string, eventJson: string, id: string, npub: string): Promise<{
        signature: string;
        id: string;
        event: string;
    }>;
    nip04Encrypt(packageName: string, plainText: string, id: string, pubKey: string, npub: string): Promise<{
        result: string;
        id: string;
    }>;
    nip04Decrypt(packageName: string, encryptedText: string, id: string, pubKey: string, npub: string): Promise<{
        result: string;
        id: string;
    }>;
    nip44Encrypt(packageName: string, plainText: string, id: string, pubKey: string, npub: string): Promise<{
        result: string;
        id: string;
    }>;
    nip44Decrypt(packageName: string, encryptedText: string, id: string, pubKey: string, npub: string): Promise<{
        result: string;
        id: string;
    }>;
    decryptZapEvent(packageName: string, eventJson: string, id: string, npub: string): Promise<{
        result: string;
        id: string;
    }>;
    getRelays(packageName: string, id: string, npub: string): Promise<{
        result: string;
        id: string;
    }>;
};
export type { AppInfo, Permission };
